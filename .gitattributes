# Normalise line endings to LF in the repo and in the working tree on every
# platform. Biome formats to LF; without this, autocrlf checkouts on Windows
# leave every file reported as "needs formatting" by `biome ci`.
* text=auto eol=lf
